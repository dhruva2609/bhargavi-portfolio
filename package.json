{
  "name": "bhargavi-portfolio-production",
  "version": "1.0.0",
  "type": "module",
  "description": "Editorial Pearl - Full Stack Deployment Wrapper",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && cd ../server && npm install --legacy-peer-deps",
    "build-client": "cd client && npm run build",
    "build": "npm run install-all && npm run build-client",
    "start": "cd server && npm start",
    "production": "npm run build && npm start",
    "healthcheck": "node scripts/healthcheck.js"
  }
}
